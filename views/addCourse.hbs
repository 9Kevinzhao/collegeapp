<h1 class="addacollege">Add course!</h1>

{{#if error}} <span>{{error}}</span>{{/if}}
<form class="addcourse" action="/users/addCourse" method="post">

  <div class="form-group">
      <label>college</label>
      <input type="text" name="college" class="form-control" placeholder="College">
  </div>
    <div class="form-group">
        <label>Course</label>
        <input type="text" name="course" class="form-control" placeholder="add addcourse">
    </div>
    <input type="submit" class="btn btn-primary" value="Save">
</form>
<a class="btn btn-default" href="/">Back</a>
